package pattern.strategy;

public class FuelCalculation {

    public float calculate(FuelConsumptionFormula fuelConsumptionFormula) {
        fuelConsumptionFormula.formula();

        return fuelConsumptionFormula.formula();
    }

}
